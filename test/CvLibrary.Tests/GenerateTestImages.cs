// Quick runner: dotnet run --project test/CvLibrary.Tests to generate test images
// Uncomment the Main method below and temporarily change <OutputType> to Exe

/*
using CvLibrary.Tests.Match;

var outputDir = Path.Combine(
    Environment.GetFolderPath(Environment.SpecialFolder.Desktop),
    "CvLibrary_TestImages");
SyntheticImageGenerator.GenerateAll(outputDir);
Console.WriteLine("Done! Press any key to exit.");
Console.ReadKey();
*/
